import React from 'react';
import ReactDOM from 'react-dom/client';
import MedicalRecordsApp from './medicalRecordsApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MedicalRecordsApp />
  </React.StrictMode>
);
